name: Reusable workflow

on:
  workflow_call:
    inputs:
      reponame:
        required: true
        type: string

jobs:
  example_job:
    name: Run shared action and show passed value
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls -a
      - uses: ./.github/actions/shared@main
      - run: echo this workflow is run by ${{ inputs.reponame }}
